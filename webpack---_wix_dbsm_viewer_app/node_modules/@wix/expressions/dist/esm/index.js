import {
    stylingFunctions
} from './stylingFunctions';
export * from './expression';
let expressionFunctions;
export const loadExpressionFunctions = async () => {
    if (expressionFunctions) {
        return;
    }
    const {
        utils,
        ...lib
    } = (await
        import (
            /* webpackChunkName: "formulajs" */
            '@formulajs/formulajs'));
    expressionFunctions = {
        ...lib,
        ...utils.symbols,
        ...stylingFunctions,
    };
};
export const getExpressionFunctions = () => expressionFunctions;
//# sourceMappingURL=index.js.map