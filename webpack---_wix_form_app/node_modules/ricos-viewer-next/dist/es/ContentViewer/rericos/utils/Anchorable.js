import React from 'react';

const Anchorable = ({
    nodeId,
    children
}) => {
    return React.cloneElement(React.Children.only(children), {
        id: `viewer-${nodeId}`
    });
};

export {
    Anchorable as
    default
};
//# sourceMappingURL=Anchorable.js.map