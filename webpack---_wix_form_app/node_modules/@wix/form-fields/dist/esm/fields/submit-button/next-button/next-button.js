import React from 'react';
import {
    ButtonSize,
    Button
} from 'wix-ui-tpa/cssVars';
import classNames from 'classnames';
import {
    classes
} from './next-button.st.css';
import {
    ForcedState,
    useForcedState
} from '../../../contexts/forced-state-context';
import {
    useFieldActions
} from '../../../contexts/field-actions-context';
const NextButton = _ref => {
    let {
        nextText,
        className
    } = _ref;
    const {
        actions
    } = useFieldActions();
    const forcedState = useForcedState();
    const hover = forcedState.includes(ForcedState.NEXT_BUTTON_HOVER);
    return /*#__PURE__*/ React.createElement(Button, {
        className: classNames(classes.navigationButtons, classes.nextButton, {
            [classes.nextButtonHover]: hover
        }, className),
        upgrade: true,
        size: ButtonSize.large,
        onClick: () => actions == null ? void 0 : actions.setNextStep()
    }, nextText);
};
export default NextButton;
//# sourceMappingURL=next-button.js.map