import {
    createContext,
    useContext
} from 'react';
export const FormViewContext = /*#__PURE__*/ createContext(undefined);
export function useFieldView(fieldId) {
    const context = useContext(FormViewContext);
    if (context === undefined) {
        throw new Error('useFieldView must be used within a FormViewProvider');
    }
    const fieldView = context.fields.find(field => field.id === fieldId);
    return fieldView;
}
//# sourceMappingURL=form-view-context.js.map