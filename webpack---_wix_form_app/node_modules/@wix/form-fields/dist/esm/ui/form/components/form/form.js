import React from 'react';
import {
    createFormFields
} from './create-form-fields';
import {
    Grid
} from '../grid/grid';
import {
    useTPAComponentsContext
} from 'wix-ui-tpa/cssVars';
import {
    FormViewContext
} from '../../../../contexts/form-view-context';
import {
    DefaultFieldLayout
} from '../../../../field-layout';
const FormRoot = _ref => {
    let {
        form,
        values,
        onChange,
        errors
    } = _ref;
    const {
        rtl
    } = useTPAComponentsContext();
    const formFields = createFormFields({
        form,
        values,
        onChange,
        errors,
        FieldLayout: DefaultFieldLayout
    });
    return /*#__PURE__*/ React.createElement(Grid, {
        isRTL: rtl,
        items: formFields,
        layout: form.grid
    });
};
export const Form = props => {
    return /*#__PURE__*/ React.createElement(FormViewContext.Provider, {
        value: props.form
    }, /*#__PURE__*/ React.createElement(FormRoot, props));
};
//# sourceMappingURL=form.js.map