import {
    useMemo
} from 'react';
import {
    useTPAComponentsContext
} from 'wix-ui-tpa/cssVars';
import {
    createFormView
} from '../../services';
export const useFormView = (form, stepId, styleParams) => {
    const {
        mobile: isMobile
    } = useTPAComponentsContext();
    return useMemo(() => {
        return (form ? .steps || [])
            .filter((step) => !stepId || stepId === step.id)
            .map((step) => {
                return createFormView({
                    form,
                    styleParams,
                    stepId: step.id,
                    isMobile,
                });
            });
    }, [form, stepId, isMobile, styleParams]);
};
//# sourceMappingURL=use-form-view.js.map