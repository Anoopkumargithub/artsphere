import * as React from 'react';
import {
    ErrorSmall
} from '@wix/wix-ui-icons-common/on-stage';
import {
    classes,
    st
} from './ErrorMessageWrapper.st.css';
import {
    ErrorMessageWrapperDataHooks,
} from './types';
export class ErrorMessageWrapper extends React.Component {
    constructor() {
        super(...arguments);
        this._getMessage = () => {
            const {
                message,
                messageId
            } = this.props;
            return (React.createElement("div", {
                    id: messageId,
                    className: classes.message,
                    "data-hook": ErrorMessageWrapperDataHooks.Message
                },
                React.createElement(ErrorSmall, {
                    className: classes.icon,
                    "aria-hidden": true
                }),
                message));
        };
    }
    render() {
        const {
            className,
            children,
            visible,
            suffixText,
            'data-hook': dataHook,
        } = this.props;
        return (React.createElement("div", {
                className: st(classes.root, {
                    visible
                }, className),
                "data-hook": dataHook
            },
            children,
            visible &&
            (suffixText ? (React.createElement("div", {
                    className: classes.messageWithSuffix
                },
                this._getMessage(),
                suffixText)) : (this._getMessage()))));
    }
}
ErrorMessageWrapper.displayName = 'ErrorMessageWrapper';
ErrorMessageWrapper.defaultProps = {
    visible: false
};
//# sourceMappingURL=ErrorMessageWrapper.js.map