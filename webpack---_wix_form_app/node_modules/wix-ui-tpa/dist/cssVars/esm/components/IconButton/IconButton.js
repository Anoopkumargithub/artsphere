import * as React from 'react';
import {
    st,
    classes
} from './IconButton.st.css';
import {
    CoreButtonNext as ButtonNext
} from '../internal/WixUiCoreComponents/components/CoreButtonNext';
import {
    deprecationLog
} from '../../common/deprecationLog';
import {
    IconButtonSkins,
    IconButtonThemes,
} from './types';
/** An implementation of an IconButton for TPAs,
 * The TPA implementation provides few extendable theme properties with the ability to override the base properties as well.
 * By default the IconButton will pull the theme from the site (site color scheme) */
export class IconButton extends React.Component {
    componentDidMount() {
        if (this.props.skin) {
            deprecationLog('IconButton', 'The prop "skin" is deprecated and will be removed in a future version, please use Toggle icons instead.');
        }
    }
    render() {
        const {
            icon,
            disabled,
            skin = IconButtonSkins.Line,
            className,
            innerRef,
            theme,
            ...rest
        } = this.props;
        return (React.createElement(ButtonNext, {
                className: st(classes.root, {
                    disabled,
                    skin,
                    theme
                }, classes[`skin-${skin}`], className),
                ...rest,
                ref: innerRef
            },
            React.createElement("span", {
                className: classes.icon
            }, icon)));
    }
}
IconButton.displayName = 'IconButton';
IconButton.defaultProps = {
    theme: IconButtonThemes.None,
};
//# sourceMappingURL=IconButton.js.map